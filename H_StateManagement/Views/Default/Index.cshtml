@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div>
 
        
        @if (Session["tut"] != null)
        {
            <h1>@Session["tut"]</h1>
        }

        @{ 
            HttpCookie k = Request.Cookies["kuk"];
            string s = "";
            if (k != null)
            {
                s = k.Value;
            }
        }
        <br />
        @s

        @using (Html.BeginForm("KukiyeAt", "Default"))
        {
            <input type="submit" value="Cookiye At...." />
        }


    </div>
</body>
</html>
