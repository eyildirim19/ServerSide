@model IEnumerable<I_ScafoldingTemplateApp.Models.Ogrenci>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Ekle")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Adi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SoyAdi)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Adi)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SoyAdi)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Duzenle", new { id = item.ID }) |
                    @Html.ActionLink("Delete", "Sil", new { id=item.ID })
                </td>
            </tr>
        }

    </table>
</body>
</html>
